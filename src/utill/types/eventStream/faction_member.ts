export type faction_member_join = {
	factionId: string
	factionName: string
	playerUuid: string
	rank: string
}

export type faction_member_leave = {
	factionId: string
	factionName: string
	playerUuid: string
}
